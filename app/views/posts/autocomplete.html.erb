<% @posts.each do |post| %>
  <li class="list-group-item list-group-item-action" 
      role="option" 
      data-autocomplete-value="<%= post.id %>" 
      data-autocomplete-label="<%= post.shop.name %>">
    <%= post.shop.name %>
  </li>
<% end %>
